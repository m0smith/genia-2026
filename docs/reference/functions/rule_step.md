# `rule_step`

**Category:** `flow` &nbsp;|&nbsp; **Arity:** 3

```genia
rule_step(record, ctx, out)
```

Replace `record`, replace `ctx`, and emit `out` in one `rules(..fns)` step.
Legacy/compatibility name; see also `step_step` (preferred).

---

_Source: `std/prelude/flow.genia` &middot; category `flow`. Generated from `@doc`/`@meta` by `tools/gen_function_docs.py`._

[<- Back to the Function Reference](../index.md)
