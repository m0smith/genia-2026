name: delay
category: ir
description: delay() special form lowers as IrDelay with a lowered body expression
input:
  source: |
    delay(x + 1)
expected:
  ir:
    - node: IrExprStmt
      expr:
        node: IrDelay
        expr:
          node: IrBinary
          left:
            node: IrVar
            name: x
          op: PLUS
          right:
            node: IrLiteral
            value: 1
