name: parse-error-r21-malformed-exponent-missing-digits
category: parse
description: R21 malformed exponent with no digits after the exponent marker is rejected deterministically
input:
  source: "1e"
expected:
  parse:
    kind: error
    type: SyntaxError
    message: "Malformed exponent"
