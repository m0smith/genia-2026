# CLI Specs

Place shared CLI behavior cases here.

Intended coverage:

- file mode
- `-c`
- `-p` / `--pipe`
- stdout/stderr/exit-code normalization
- `argv()` contract

Current status:

- directory scaffold only
