id: r13-cross-mode-command
name: r13-cross-mode-command
category: cli
description: Command mode requires explicit R13 construction and preserves qualified lookup Outcomes
input:
  source: &source |
    provider = config_standard({SERVER_PORT: "8080"}, []) |> unwrap_or(none)
    config_view(provider, "SERVER_")("PORT")
  command: *source
  stdin: ""
  argv: []
  debug_stdio: false
expected:
  stdout: 'some("8080")'
  stderr: ""
  exit_code: 0
notes: |
  issue: 675
  classification: Experimental command-mode observation over ordinary calls.
