name: error-strip-representation-reserved-secret
category: error
description: Generic stripping rejects the reserved secret facet without inspecting its subject
input:
  source: |
    strip_representation("secret", "PAYLOAD_SENTINEL_591")
expected:
  stdout: ""
  stderr: "Error: strip_representation cannot use reserved protected facet \"secret\"\n"
  exit_code: 1
notes: |
  error_phase: eval
  error_category: TypeError
