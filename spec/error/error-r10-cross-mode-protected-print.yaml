name: error-r10-cross-mode-protected-print
category: error
description: Explicit protected acquisition is rejected by stdout without disclosing fixture sentinels
input:
  source: |
    provider = config_provider([{kind: quote(values), values: {R10_CROSS_MODE_KEY_SENTINEL_594: "R10_CROSS_MODE_PAYLOAD_SENTINEL_594"}}]) |> unwrap_or(none)
    token = secret_get(provider, "R10_CROSS_MODE_KEY_SENTINEL_594", quote(fixture_use)) |> unwrap_or(none)
    print(token)
expected:
  stdout: ""
  stderr: "Error: protected-value: print\n"
  exit_code: 1
