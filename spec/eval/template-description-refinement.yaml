name: template-description-refinement
category: eval
description: refinement(predicate) returns an inspectable Template matching refinement_match behavior
input:
  source: |
    Positive = refinement((x) -> x > 0)
    [Positive(5), Positive(-1), template_description(Positive)]
expected:
  stdout: "PLACEHOLDER\n"
  stderr: ""
  exit_code: 0
