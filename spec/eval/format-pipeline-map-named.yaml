name: format-pipeline-map-named
category: eval
input:
  source: |
    range(3)
    |> map((x) -> {n:x})
    |> map((x) -> format("number:{n}", x))
    |> collect
expected:
  stdout: |
    ["number:0", "number:1", "number:2"]
  stderr: ""
  exit_code: 0
