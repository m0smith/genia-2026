name: first-on-flow-type-error
category: eval
input:
  source: |
    inc(n) -> n + 1
    first(evolve(0, inc))
expected:
  stdout: ""
  stderr: |
    Error: No matching case for function first/1 with arguments (<flow evolve ready>,)
  exit_code: 1
