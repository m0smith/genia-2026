name: seq-compatible-range-transform-chain
category: eval
input:
  source: |
    range(5)
      |> map((x) -> x + 1)
      |> collect
expected:
  stdout: "[1, 2, 3, 4, 5]\n"
  stderr: ""
  exit_code: 0
