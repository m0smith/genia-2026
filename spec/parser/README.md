# Parser Specs

Place shared parser-facing cases here.

Intended coverage:

- accepted syntax snapshots
- rejected syntax snapshots
- case placement constraints
- pattern validity constraints
- docstring parse invariants

Current status:

- directory scaffold only
