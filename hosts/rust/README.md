# Rust Host

Status:

- planned
- no host implementation exists yet in this repo

Goal:

- add a Rust host that preserves the shared Genia semantics defined by the root docs, host-interop docs, and shared spec artifacts

Rules:

- shared docs/specs are authoritative
- host-local convenience must not override shared semantics
- do not claim this host is implemented until code, tests, and capability-matrix status all exist

Known commands:

- setup: TODO
- build: TODO
- test: TODO
- lint: TODO
