# Parse Adapter Blackbox Fixtures

These YAML fixtures are host-local test data for `tests/test_spec_parse_runner_blackbox.py`.

They are not part of the active shared semantic-spec suite in phase 1.
