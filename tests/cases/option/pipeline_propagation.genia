inc(x) = x + 1
inc_opt(x) = some(x + 1)
stop(x) = none("parse-error", { source: "stop" })

[
  none?(some(4) |> inc),
  none?(3 |> inc_opt |> inc),
  none?(none("empty-list") |> parse_int),
  none?(3 |> stop |> inc),
  ((3 |> inc) |> inc)
]
